apiVersion: v2
name: k8s-pv-mount-point-exporter
description: Kubernetes PersistentVolume MountPoint Exporter

type: application

version: 1.0.3

appVersion: "1.0.3" # Will be used in GitHub Actions to set Container image tag and update helm chart "image.tag"
